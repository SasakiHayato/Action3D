/// <summary>
/// グローバルスペースで使われる列挙型
/// </summary>

public enum AttackType
{
    Sword,
    Bullets,

    None,
}

public enum ColorType
{
    Player,
    Enemy,
}

public enum UIType
{
    Player,
    Game,
    EnemyConnect,
    Title,
}

public enum InputType
{
    PlayerMove,
    CmMove,
    Select,
    ShotVal,
}